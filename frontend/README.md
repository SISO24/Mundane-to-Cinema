# Frontend (React + Vite)

Single-page app: type a mundane task, get a cinematic poster, download it.

## Setup
```bash
npm install
cp .env.example .env   # point VITE_API_URL at your backend
npm run dev
```
